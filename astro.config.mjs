import { defineConfig } from 'astro/config';

export default defineConfig({
  // This tells Astro your website is in a subfolder called /Paul-chatterton/
  site: 'https://bbgydjqvxb-glitch.github.io',
  base: 'Paul-chatterton',
  output: 'static',
});
